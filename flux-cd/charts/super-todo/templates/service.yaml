apiVersion: v1
kind: Service
metadata:
  name: {{ include "super-todo.fullname" . }}
  labels:
    {{- include "super-todo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "super-todo.selectorLabels" . | nindent 4 }}
